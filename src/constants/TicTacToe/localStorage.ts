const gamePrefix = "TICTACTOE";
const LS_BOARD = `${gamePrefix}-board`;
const LS_TURN = `${gamePrefix}-turn`;
const LS_WINNER = `${gamePrefix}-winner`;
const LS_WINNER_CONDITION = `${gamePrefix}-winner-condition`;

export { LS_BOARD, LS_TURN, LS_WINNER, LS_WINNER_CONDITION };
